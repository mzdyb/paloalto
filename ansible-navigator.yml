---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: aap.rh.lab/ee-panos:1.0
    pull:
      policy: missing
  mode: stdout
  playbook-artifact:
    enable: false
