---
ansible-navigator:
  ansible:  
    config:
      path: ansible.cfg
  execution-environment:
    image: hub.rh.lab/ee-panos:1.3
    pull:
      policy: missing
  playbook-artifact:
    enable: false
